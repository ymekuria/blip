import { AsyncStorage } from 'react-native';
import { Facebook } from 'expo';

// using redux think for async actions 
export const fbLogin = async dispatch => {
	// let login = await Facebook.logInWith
}
